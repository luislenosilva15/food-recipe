import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        margin: 10,
        alignItems: 'center',
        justifyContent: 'center'
    },
    creatorImage: {
        width: 80,
        height: 80,
        borderRadius: 80
    },
    titleContainer: {
        margin: 10,
        alignItems: 'center',
        justifyContent: 'center'
    }
});